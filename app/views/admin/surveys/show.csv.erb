<%= CSV.generate_line ['survey', 'participant_key', 'from', 'to', 'direction', 'time', 'message'] -%>
<%- @survey.text_messages.order('created_at').find_each do |m| -%>
<%= CSV.generate_line([@survey.name, m.participant_external_key, m.from.humanize, m.to.humanize, m.direction, m.delivered_at, m.message]).html_safe -%>
<%- end -%>