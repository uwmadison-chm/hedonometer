= form_for [:admin, @new_admin], builder: TwoColumnFormBuilder do |f|
  = f.email_field :email
  = f.password_field :password
  = f.submit "Create admin"

%table
  %tr
    %th Email
    %th
    %th
  = render Admin.active
  = render Admin.inactive
